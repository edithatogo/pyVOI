# HPC and Julia delivery observation — 6 September 2026

## Julia upstream delivery

The existing [Yggdrasil PR 14292](https://github.com/JuliaPackaging/Yggdrasil/pull/14292)
was updated in place to commit `e479eeea400e65f0e2b78f4fb4a3fc9ed99200fa`.
The only recipe changes are version 2.1.0 → 2.2.0 and GitSource → signed
release commit `7af563c8cb373057d30662650b3f332f39e05b83`. The uploaded bytes
match `packaging/yggdrasil/candidates/v2.2.0/build_tarballs.jl`. The negative
platform filters and prior platform-specific exclusions remain intact.

Fresh Buildkite checks were pending when observed. The 15 successful platform
jobs at the previous head `2528e2efb90e4197924d45c98873ca5cdb1a9d42`
are historical v2.1.0 evidence, not v2.2.0 qualification. The dated 31 August
candidate receipt remains an immutable pre-delivery observation; this record
supersedes its statement that no upstream update has occurred.

Julia syntax parsing passed. The complete focused test invocation exited 0:

```sh
.venv/bin/python -m pytest tests/test_julia_binarybuilder_readiness.py tests/test_yggdrasil_platform_coverage_contract.py tests/test_julia_registry_followthrough.py -q
```

The isolated `/tmp/voiage-hj-frontier` source snapshot passed `uv lock --upgrade`
and `scripts/dependency_frontier.py --strict`. Its report is under
`.conductor/local/dependency_frontier.md`; no exploratory lock updates were
applied to the shared repository. This is dependency-policy evidence, not a
numerical-equivalence claim. The integrated full tox gate remains required.

Issues #555 and #1023 remain open until fresh products, an actual JLL identity,
clean-depot execution without a manually supplied library, and General
registration are evidenced. No JLL UUID or registry acceptance was invented.

## HPC recovery and verified source repair

Both existing Lima HPC VMs were stopped at initial inspection. The internal
VM at `/Users/doughnut/.lima-voiage-internal/hpc` was started successfully;
no duplicate Spack/EasyBuild process existed. Its retained terminal receipt
records a failed Spack source installation at candidate
`067fec9d5cf2baf2abea5f78a7a41fdfe4503617`: both derived Polars runtime URLs
returned HTTP 404. The retained store and failure evidence were preserved.

At `2026-09-06T13:17:53.416474+00:00`, the corrected exact source URL already
in the current overlay was downloaded successfully inside that Linux VM:

- Archive: `polars_runtime_32-1.42.1.tar.gz`, 3,045,460 bytes.
- SHA-256: `4d4809e1c1b9a6611f6944f27b24abea902b5159e6b6fa262fd716e947af5afd`.
- VM-local receipt/cache: `~/voiage-hpc-evidence/source-repair-20260906/`.
- Native installation and numerical/module probes have not passed.

This closes the source-access diagnostic, not native qualification. The next
Spack attempt must bind the corrected overlay to a fresh candidate receipt
and may reuse the existing store without altering the failed receipt.

The VM has four CPUs and a 200 GiB disk (163 GiB free observed). The native
EasyBuild driver requires eight CPUs and 250 GiB free. Neither generation
was launched under an inadequate configuration, and those checks were not
weakened. Capacity must be provided before the sequential 2023a/2024a native
matrix; upstream HPC publication remains pending. The existing VM is left
running for continued qualification, with no background build launched.

## Fresh Spack retry preflight

A fresh driver/source export is prepared inside the existing internal VM at
`~/voiage-hpc-source-repair-20260906/`, bound to candidate
`2187a93257ba16b3dadfa65a5951f60edb54020e` and overlay manifest SHA-256
`0a1b84267c7eb01eb5359fdd08c053c65013ec621d4fc6069da0053daaee4446`.
Driver SHA-256 is
`f1102faa6827464bfdd7708817cc921636f6b4b335500824650a316e581c68f1`;
it preserves the prior failure chain, validates the overlay and concrete DAG,
limits compilation to two jobs, holds the retained-store lock, and records a
new receipt rather than rewriting old observations.

The preflight receipt at `preflight-receipt.json` records
`not_started_capacity`, not a test failure: at 13:20:32 UTC the internal host
volume had 13,838,843,904 bytes free, below the 25 GiB safety reserve. A sparse
VM disk reporting 163 GiB available cannot overcome its physical backing
volume's limit. Guest memory was adequate (11,561 MiB available).

The PortableSSD backing volume had approximately 317 GiB free, but its older
VM remains stopped after the previously recorded revoked disk handle/frozen
hostagent incident. It is not silently substituted for the reviewed internal
storage path. No disk migration, deletion, or native compilation was started.
Reclaiming sufficient internal capacity or qualifying recovered external VM
storage is now the concrete prerequisite for the guarded Spack retry.

## Portable VM recovery and actual retry

The existing portable VM subsequently passed a bounded recovery boot: SSH
and guest agent became ready, the disk had a normal live (not revoked) file
handle, and no previous build process remained. The guest reported 165 GiB
free and 11,565 MiB available memory; the host backing volume retained over
300 GiB free. No disk was migrated or deleted. Existing virtiofs mounts remain
configured, but new sources, build state and receipts use guest-local ext4.

At 13:26:37 UTC an actual fresh Spack attempt began, retaining the existing
store with an exclusive lock. It uses the same candidate and overlay above
with a separately hashed portable-recovery driver
`b6c4be7a6f2af9c81390716e05934e1e0bf89c2e1d1afcc96b0f407cd26a6bbe`.
The new receipt is `~/voiage-hpc-evidence/spack-20260906T132637Z/receipt.json`
in the portable VM. It explicitly records recovered portable backing and
configured external mounts, and imports the internal Polars failure receipt
under a separate filename without altering historical observations.

The host watchdog checks backing-file identity and a 25 GiB physical free-space
reserve every ten seconds. The guest driver enforces its separate 25 GiB
reserve, two compilation jobs and 24-hour deadline. Git fetch, checkout, exact Spack identity/version, compiler discovery, fresh
concretization and concrete-DAG validation passed. Source installation is
running and has installed multiple Python dependencies successfully. This is
an active qualification attempt,
not an installed-probe success. Host console:
`/tmp/voiage-portable-build-console-20260906.log`.

Also, the fresh v2.2.0 [Buildkite matrix 32555](https://buildkite.com/julialang/yggdrasil/builds/32555)
now reports success for all 15 platform jobs at upstream head `e479eeea`.
Yggdrasil PR 14292 remains open; successful build checks do not establish JLL
generation or General registration.
