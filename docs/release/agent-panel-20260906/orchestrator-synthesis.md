# Orchestrator synthesis — 6 September 2026

Author: `/root`, separate from the assessment agent `/root/scientific_panel`
and challenge agent `/root/coverage_repro`. These are agent assessments with
shared model/tool risks, not independent human validation. The sole maintainer
retains the scientific and product decision. No other human is recruited.

## Reviewed scope and evidence

The candidate remains the proposed exclusion of an automatic scalar or
study-authorizing sampling-harm kernel, with narrower constrained, scalar and
vector research preserved. It is not an estimator implementation. The review
and challenge bind candidate `2187a93257ba16b3dadfa65a5951f60edb54020e` and
the input hashes listed in `scientific-findings.json`. The packet manifest
binds these additive assessments without changing historical dispositions.

The initial review addresses every one of the nineteen findings through five
declared role lenses. The second agent challenges each recommendation. Neither
claims five independently commissioned reviewers. Three full primary-source
editions were subsequently retrieved and assessed in `source-body-review.json`;
the earlier failed/abstract-only observations remain historical and do not
override those later receipts. Full texts are not redistributed in this PR.
Project 28 owner-role fields now explicitly name agents and the sole maintainer;
`project-readback.json` preserves the actual remaining evidence/risk states.

## Agreement, dissent and disposition

Both agents support retaining the non-authorizing boundary. Their agreement is
useful scrutiny, not proof of general scientific safety. The source evidence
supports preserving a constrained research path and considering the full
decision context; it does not establish that all harm can never be valued.
The proposed exclusion must therefore be understood as a repository capability
boundary, never a universal impossibility theorem.

I accept the challenge that future candidate requirements need falsifiable
elicitation, missingness/attribution bounds, adversarial tails and explicit
failure rules. Those requirements remain prerequisites for a future applied
estimator, rather than a request to invent a disease, jurisdiction, numerical
threshold or study protocol to keep an unsupported capability unavailable.

I reject restoring additional-human recruitment from historical contracts.
The user's current policy expressly supersedes that remedy. The limitation
that agents are not independent humans remains disclosed; the remedy is
separate agent challenge and sole-maintainer decision, not fabricated human
qualifications. External ethics/regulatory authority for a real study is a
different fact and is not conferred by this repository decision.

The non-alias delta is implemented and tested. It proves the effective issue
mapping and execution boundary, not an entire scientific equivalence proof.
Fresh Project observations advance administrative re-review, but do not
retroactively renew the expired historical administrative receipt.

Critical H8D-DS-03 remains unresolved for runtime or study authorization. No
implemented sampling-harm estimator, rescue process or halt authority exists.
Positive expected economic value cannot clear an undefined catastrophic
constraint. All nineteen historical dispositions remain pending until the
candidate-bound accountable disposition is recorded.

## Next executable slice

Complete the remaining source-specific edition/rights/applicability entries,
and turn the scope wording into an explicit candidate-bound proposed decision
with preserved dissent. Then present one maintainer decision: accept the
limited repository exclusion while retaining all future runtime prerequisites,
or request a concrete revision. No repeated declarations are needed now.

This packet advances #876, #853 and #850 but does not close them, authorize
runtime or real-study activity, promote maturity, publish a release or assert
venue acceptance. The implemented safeguards remain in force throughout.
