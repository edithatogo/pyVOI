# Sampling-acquisition-harm review, 6 September 2026

This is one agent's evidence-bound review of issues #876, #853 and #850, organized
into five role lenses. It is not five independent reviewers, a completed panel,
an accountable scientific disposition, or a source/ethics authorization. A
separately authored challenge and orchestrator synthesis must follow. The sole
maintainer supplies the final repository decision; no additional human reviewer
recruitment is requested.

The reviewed object is the proposed exclusion of a generic automatic-scalar or
study-authorizing kernel. It is not an executable estimator. The nineteen
historical findings and their severity remain intact in `scientific-findings.json`;
that file supplies a concrete next action for every finding without rewriting
historical pending dispositions. Input digests and the candidate branch commit
bind this review. A changed input invalidates the affected assessment.

## Estimand and scientific scope

The estimand boundary's restriction on undeclared scalarization is justified as
an API contract: adding a monetary information value to an unweighted outcome in
another unit is not a defined scalar estimand. It is not a proof that all harm
valuation is impossible. A declared component ledger, units, horizon, perspective,
weights and non-overlap assumptions could define a narrower model. This preserves
H8D-EST-003 dissent and the candidate-specific scalar, constrained and vector
research pathways already listed in `review-candidate.json`.

H8D-ED-01/02/03 need not cause us to invent an applied population merely to keep a
capability unsupported. They remain prerequisites for any future applied kernel.
A zero numeric harm input is not evidence of zero acquisition harm: dropout,
changed outcomes and double counting must be assessed in that future model.

## Estimator assurance

No estimator, truth-known fixture or false-safe error budget exists for this
capability. Consequently this review makes no accuracy, calibration or tail-risk
claim. Testing ordinary ENBS would not remediate H8D-EST-001. A future proposal
needs its own measurable assurance targets and must treat an undefined harm law
as indeterminate, rather than substituting zero.

H8D-EST-004's human-recruitment remedy is superseded by the user's panel policy;
the historical limitation is not retroactively erased. Distinct agent challenges
can improve scrutiny but are not independent human validation and can share
failure modes. This single report does not complete that replacement panel.

## Cross-language API

H8D-API-GOV-01 has substantive implemented evidence: the additive
`adjacent-method-non-alias-delta.json` names exactly #570, #571, #595 and #598,
with execution reuse false. The remediation loader validates the effective
projection while retaining the frozen two-entry base. Existing mutation tests
exercise duplicate issues, missing issues, execution reuse and byte drift.
Changing that historical base merely to make it contain four entries would
break its deliberate digest contract. No Python/native/R/Julia/Mojo sampling-harm
runtime is thereby created.

## Source and governance assurance

The source register still reports zero retained source bodies across six records.
This review did not retrieve new primary-source bytes and makes no current legal,
medical, ethical or source-rights determination. H8D-ED-04, EST-002, API-GOV-03,
GP-02 and DS-05 share a source-evidence dependency: collect one lawful,
versioned receipt packet, then assess distinct scientific/domain claims against
it. Do not perform six duplicate retrieval campaigns or pretend an HTTP success
establishes rights or applicability.

API-GOV-02 and GP-01 correctly separate historical and current governance. Their
old administrative delta expired on 9 August; its tests intentionally validate
at historical time. Fresh issue bodies are retained in
`scientific-issue-readback.json`, including the current sole-maintainer policy.
Project28 was not refreshed here, so synchronized Project readback remains open.
API-GOV-04 no longer requires different humans, but still requires honest agent
identities, separate assessments/synthesis and a sole-maintainer receipt.

## Domain and safety

H8D-DS-03 remains Critical for any executable or study-authorizing candidate.
Expected economic benefit cannot discharge an unspecified catastrophic constraint.
The safe current result is no runtime and no study authorization, not a fabricated
residual-risk tolerance. Monitoring sensitivity, stopping, rescue, escalation and
halt authority must describe a real future candidate before claiming operational
safety. DS-01/02/04 likewise remain applied-candidate prerequisites, not grounds
to fabricate a jurisdiction, consent process or affected-party ledger today.

## Recommendation for the separate orchestrator

Advance the limited exclusion as a repository scope decision, keeping every
runtime and study-authority flag false and preserving the narrower research path.
Do not close #876/#853/#850 from this report alone. Obtain a separate agent
challenge, complete the source receipt/application gap where the exclusion relies
on source claims, reconcile current Project fields, and bind the resulting
recommendation to a new packet. Only then request one maintainer disposition.
Distinguish exclusion acceptance from validation of any future estimator: the
latter still has substantial scientific and operational prerequisites.

Validation of existing contract behavior is recorded separately. Full repository
tox validation belongs to the integrated candidate and is not claimed here.
